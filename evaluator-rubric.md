# FloatVocab 评审评分表

每轮（或每几轮）会话后使用。六个维度各 0–2 分，满分 12 分。

---

## 评分维度

### 1. 正确性（0–2）

实现出来的行为是否符合 `feature_list.json` 中该功能的 `user_visible_behavior`。

| 分 | 标准 |
|----|------|
| 2 | 用户可见行为与描述完全一致，边界情况（空词库、网络断开、文件格式错误）处理正确 |
| 1 | 主路径正确，但边界情况有漏洞或与描述有轻微偏差 |
| 0 | 主路径不正确，或功能根本无法使用 |

**通过线**：≥ 1

---

### 2. 验证（0–2）

`feature_list.json` 中 `verification` 步骤是否真的逐一执行，并留下 `evidence`。

| 分 | 标准 |
|----|------|
| 2 | 所有 verification 步骤执行完毕，evidence 包含具体输出（测试结果、截图描述、命令输出） |
| 1 | 大部分步骤执行，evidence 存在但不完整 |
| 0 | evidence 为空，或 verification 步骤未执行就标记 passing |

**通过线**：≥ 1（evidence 必须非空）

---

### 3. 范围纪律（0–2）

是否基本保持在选定功能范围内，没有跑偏修改其他功能。

| 分 | 标准 |
|----|------|
| 2 | 修改完全在目标功能范围内，或额外改动有明确记录且必要 |
| 1 | 有少量范围外改动，但无破坏性影响 |
| 0 | 大量范围外改动，或修改了其他功能的核心逻辑导致回归 |

**通过线**：≥ 1

---

### 4. 可靠性（0–2）

结果是否能在重启或重跑后继续工作。

| 分 | 标准 |
|----|------|
| 2 | 重启应用后功能状态（包括持久化数据）完整保留，`python -m pytest tests/ -v` 全绿 |
| 1 | 功能工作但持久化有问题，或测试有少量 skip |
| 0 | 重启后功能失效，或测试有 FAILED / ERROR |

**通过线**：≥ 1（测试必须无 FAILED）

---

### 5. 可维护性（0–2）

代码和文档是否清楚到足以交给下一轮会话。

| 分 | 标准 |
|----|------|
| 2 | 遵循 repositories/services/models 三层分离；无多余注释和调试代码；代码意图通过命名自明 |
| 1 | 结构基本合理，有少量不规范之处 |
| 0 | 直接在 app.py 操作 DB、大量 print 调试代码、命名混乱 |

**通过线**：≥ 1

---

### 6. 交接准备度（0–2）

新会话是否能只靠仓库内文件（不需要口头说明）继续推进。

| 分 | 标准 |
|----|------|
| 2 | `claude-progress.md`、`session-handoff.md`、`feature_list.json` 全部更新；下一步动作明确 |
| 1 | 大部分文件更新，但缺少某个关键字段 |
| 0 | 交接文件未更新，下一轮 agent 无法判断从哪继续 |

**通过线**：≥ 1

---

## 汇总

| 维度 | 得分 | 通过线 |
|------|------|--------|
| 正确性 | /2 | ≥1 |
| 验证 | /2 | ≥1 |
| 范围纪律 | /2 | ≥1 |
| 可靠性 | /2 | ≥1 |
| 可维护性 | /2 | ≥1 |
| 交接准备度 | /2 | ≥1 |
| **总分** | **/12** | **≥8** |

---

## 结论

- **Accept**：总分 ≥ 8，且所有维度 ≥ 1（无维度为 0）
- **Revise**：总分 6–7，或有 1 个维度为 0 但不影响主路径
- **Block**：总分 < 6，或验证/可靠性维度为 0（测试失败 / evidence 为空即 block）

---

## 校准记录

| 日期 | 被评 sprint | Evaluator 结论 | 人工判断 | 差异及调整 |
|------|-------------|----------------|----------|------------|
| — | — | — | — | — |

> 预计需要 3–5 轮校准。每次对比 evaluator 分数和人工评审，在「标准」列写更具体的通过/失败条件。
